[package]
name = "entity"
version = "0.1.0"
edition = "2021"

[lib]
name = "entity"
path = "src/lib.rs"

[dependencies]
chrono = {workspace = true}
uuid = {workspace = true}

[dependencies.sea-orm]
version = "2.0.2"
features = ["with-uuid", "with-chrono"]
