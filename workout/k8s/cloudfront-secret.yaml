apiVersion: v1
kind: Secret
metadata:
  name: cloudfront-key
  namespace: workout-app
type: Opaque
stringData:
  # Placeholder only - never commit a real private key here. Inject the
  # real CloudFront signing key out-of-band at deploy time (e.g.
  # `kubectl create secret generic cloudfront-key --from-file=...`), never
  # via this templated stringData block.
  cloudfront_private_rsa.pem: |
    -----BEGIN PRIVATE KEY-----
    CHANGE_ME
    -----END PRIVATE KEY-----

