# Base URL for all API calls. Must point at the nginx gateway (development/nginx.conf),
# never directly at a backend service (workout:8090, timeline:8091) — the gateway is what
# routes /login, /auth/, /workout/api/, /timeline/api/ to the right service.
VITE_API_BASE_URL=https://localhost
