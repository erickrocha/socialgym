# Copy to .env.local (gitignored, matched by *.local) to override the gateway URL
# without touching the committed .env — e.g. to test through the ngrok tunnel:
VITE_API_BASE_URL=https://latently-bubaline-salome.ngrok-free.dev
