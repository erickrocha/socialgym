apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    {{- include "socialgym.labels" . | nindent 4 }}
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: {{ .Values.postgres.port }}
      targetPort: postgres
