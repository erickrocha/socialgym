apiVersion: v1
kind: Secret
metadata:
  name: postgres
  labels:
    {{- include "socialgym.labels" . | nindent 4 }}
type: Opaque
data:
  POSTGRES_PASSWORD: {{ include "socialgym.secretValue" (list . "postgres" "POSTGRES_PASSWORD" .Values.postgres.password 24) | b64enc }}
