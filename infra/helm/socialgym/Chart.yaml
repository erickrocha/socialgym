apiVersion: v2
name: socialgym
description: SocialGym backend stack (gateway, Postgres, Mongo, workout, integration, timeline) for local Kubernetes via minikube
type: application
version: 0.1.0
appVersion: "1.0.0"
